server:
  port: 8081

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: customerdb

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/