server:
  port: 8079

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sergiorivasmedina/config-server.git
          search-paths:
          - config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: sergio